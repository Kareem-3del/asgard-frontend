import {useEffect} from "react";
import {Scrollbar} from 'smooth-scrollbar-react';
const SmoothScrollbar = ()=>{

    return <Scrollbar damping={0.1}>
        your contents here...
    </Scrollbar>;
}

export default SmoothScrollbar;
